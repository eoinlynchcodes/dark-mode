import React from 'react';
import { useLocalStorage } from './useLocalStorage';

export const useDarkMode = () => {

// Access dark.mode class.
// Change style to darkmode on change of button.

const accessor = document.getElementsByClassName("root");

accessor.onClick(('', ) => {
    
})


}